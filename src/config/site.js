/**
 * Site-wide constants for the marketplace / support side of the platform.
 *
 * The Gasabo Real Estate portal (src/modules/realestate/*) is deliberately
 * NOT covered by this - it carries its own brand and its own
 * gasaborealestaterwanda@gmail.com contact address.
 */

// Public "contact us" address. Shown on every marketplace and support page
// (footer, Help Center, FAQ, About, Terms, Contact) and used as the
// reply-to / recipient hint for the contact form.
export const CONTACT_EMAIL = 'kigalimarket20@gmail.com';

// Public phone / WhatsApp line, same scope as CONTACT_EMAIL.
export const CONTACT_PHONE = '+250 788 350 555';
